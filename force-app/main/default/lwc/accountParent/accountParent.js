import { LightningElement, track } from "lwc";
//import getContactByAccountId from "@salesforce/apex/ContactPageController.getContactByAccountId";

export default class AccountParent extends LightningElement {
  @track contacts;

  //@wire getContactByAccountId;
}